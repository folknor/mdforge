# Test 10: Block elements in headers
# Verify: Do block elements (divs, paragraphs) work or get flattened?
# The markdown parser might create block elements - do they display correctly?

header:
  left: "Normal text"
  center: "**Bold** then *italic*"
  right: "Mixed **bold *and italic*** text"
footer:
  center: "Footer with **bold** text"

pdf_options:
  format: A4
  margin: 20mm

# Test 1: Basic variables
# Verify: {page}, {pages}, {title}, {date} all render correctly

header:
  left: "{title}"
  right: "{date}"
footer:
  center: "Page {page} of {pages}"

pdf_options:
  format: A4
  margin: 20mm

# NPM IS DISABLED FOR THIS PROJECT
# USE PNPM INSTEAD

# This file intentionally left with warning to prevent npm usage
# All package management must be done through pnpm

# If you see this file, you're using the wrong package manager!
# Run: pnpm install

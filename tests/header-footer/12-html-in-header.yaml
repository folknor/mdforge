# Test 12: Raw HTML in headers
# Verify: Can we use HTML tags in header content?
# Note: Markdown is parsed with parseInline, so block elements might not work

header:
  left: "<span style=\"color: red\">Inline HTML</span>"
  center: "<strong>HTML strong</strong>"
  right: "<em>HTML em</em>"
footer:
  center: "Does inline HTML work?"

pdf_options:
  format: A4
  margin: 20mm

# Using a Config File

This example demonstrates using a separate `config.yaml` file.

## When to Use Config Files

Config files are useful when:

- Multiple documents share the same settings
- You want to keep markdown files clean
- Settings are managed separately from content

## Running This Example

```bash
mdforge --config-file config.yaml document.md
```
